```
sudo apt update
sudo apt upgrade -y
sudo apt install htop
sudo apt-get install software-properties-common
sudo apt update; sudo apt-get install openjdk-16-jdk-headless

java -Xmx1024M -Xms1024M -jar server.jar nogui
```
